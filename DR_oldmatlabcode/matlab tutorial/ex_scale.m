function newx = ex_scale(x,oldmin, oldmax, newmin, newmax)
% Function ex_scale takes vector x, with min and max and
% creates new min and max... outputs the new array.  Format:
%
% output=ex_scale(vector, old minimum, old maximum, new min, newmax)

