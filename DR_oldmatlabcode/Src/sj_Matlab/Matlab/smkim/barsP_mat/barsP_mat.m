% function barsP_mat(data,trials,paramfile,priorfile)
%
% data: a 2 column double matrix, column 1 are x values, equally spaced,
% and column 2 are the corresponding y values positive numbers
%
% trials (optional, default=1): the number of trials making up the histogram
%
% paramfile (optional, default='example.params'): a string indicating the 
% parameter file, as documented in the BARS material
%
% priorfile (optional, default='example.prior'): a string indicating the
% prior file