function model= adaptFilter(data, model, filter)
%function model= adaptFilter(data, model, filter)
% 
% Run adaptive filter filtering algorithm
%
% data.time     real time (sec)
% data.linpos   linearized position (cm)
% data.xpos     x coordinate (cm)
% data.ypos     y coordinate (cm)
% data.phase    theta phase (0,2*pi)
% data.traj     (-1: invalid, 0,1, ... )
%
% data.spiketime    time of spikes (sec)

error(['This is just a dummy file for the mexglx file. ' ... 
    'Compile AdaptFilter/main/adaptFilter.cc instead.']);
